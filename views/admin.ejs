<!DOCTYPE html>
<html>
<head>
    <title>Admin - Fantasy Book Voting</title>
    <style>
        body {
            background-color: #1a1a1a;
            color: #ddd;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            text-align: center;
            padding: 30px;
        }
        h1 {
            color: #9b59b6;
            margin-bottom: 20px;
        }
        form {
            background-color: #2c2c2c;
            padding: 20px;
            display: inline-block;
            border-radius: 10px;
        }
        input[type="password"], input[type="file"], button {
            display: block;
            margin: 15px auto;
            padding: 10px;
            border-radius: 5px;
            border: none;
        }
        button {
            background-color: #9b59b6;
            color: #fff;
            cursor: pointer;
            transition: background 0.3s;
        }
        button:hover {
            background-color: #7a3490;
        }
        a {
            color: #9b59b6;
            text-decoration: none;
        }
        a:hover { text-decoration: underline; }
    </style>
</head>
<body>
    <h1>Admin Page - Fantasy Book Club</h1>
    <% if (message) { %>
    <p style="color: lightgreen; font-weight: bold;"><%= message %></p>
<% } %>
    <form action="/admin" method="POST" enctype="multipart/form-data">
        <label>Password: <input type="password" name="password" required></label>
        <label>Upload Excel: <input type="file" name="excel" accept=".xlsx" required></label>
        <button type="submit">Import Books</button>
    </form>
    <br>
    <a href="/admin/results">View Results</a>
    <br><br>
    <a href="/admin/reset" style="display:inline-block; padding:10px 20px; background:#9b59b6; color:#fff; border-radius:5px; text-decoration:none;">Reset Votes</a>
</body>
</html>
